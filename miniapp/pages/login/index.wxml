<view class="page login-page">
  <view class="login-brand">
    <view class="login-logo">
      <image class="login-logo-img" src="/assets/logo.png" mode="aspectFit" />
    </view>
    <view class="login-title">萌姨萌嫂服务助手</view>
    <view class="login-desc">客户请通过合同或考勤链接进入，服务人员可在此登录并绑定员工身份。</view>
  </view>

  <view class="login-actions">
    <view wx:if="{{enableMockLogin}}" class="dev-card">
      <text class="label">开发联调 openid</text>
      <input class="input" value="{{openid}}" bindinput="onOpenidInput" />
    </view>

    <button class="btn-primary login-btn" loading="{{loading}}" bindtap="login">一键登录</button>
  </view>
</view>
